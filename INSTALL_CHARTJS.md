# Install Chart.js for Dashboard

Run this command in the major-app directory:

```bash
cd major-app
npm install chart.js
```

After installation, restart your Angular development server:

```bash
ng serve
```

Then navigate to `/admin/dashboard` to see the new dashboard design!
